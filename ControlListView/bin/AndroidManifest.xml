<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controllistview"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.controllistview.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="SimpleListView" android:label="简单的ListView列表"></activity>
        <activity android:name="TitleList" android:label="带标题的ListView列表"></activity>
        <activity android:name="IconList" android:label="带图片的ListView列表"></activity>
        <activity android:name="ColorList" android:label="自定义BaseAdapter颜色"></activity>
        <activity android:name="ArrayList" android:label="自定义ArrayAdapter颜色"></activity>
        
        
    </application>

</manifest>
